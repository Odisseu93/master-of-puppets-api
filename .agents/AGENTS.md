# Project Rules

## Git and Verification
- Before committing any changes, you must run the project's tests (`npm test` or `npm run test`) and verification checks (linting with `npm run lint`).
- Only proceed with committing code if all tests and lint checks pass successfully. If there are failures, fix them before making a commit.
